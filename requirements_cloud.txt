# Ecosystem - CLOUD REQUIREMENTS
# For cloud GPU deployments (VastAI, RunPod).
#
# PyTorch is intentionally excluded — provided by the cloud base image.
# The base image is expected to supply a torch build matching the container's
# CUDA toolkit. installer.py will reinstall torch with cu121 if it is missing
# or compiled for an incompatible CUDA version.
#
# Install with:
#   pip install -r requirements_cloud.txt

# ========== BASE DEPENDENCIES ONLY ==========
-r requirements_base.txt
