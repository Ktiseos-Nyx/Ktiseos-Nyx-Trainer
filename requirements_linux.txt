# Ktiseos-Nyx-Trainer - LINUX REQUIREMENTS
# For local Linux installations with NVIDIA GPU + CUDA 12.1
# Includes PyTorch with CUDA 12.1 support

# Install with:
#   pip install -r requirements_linux.txt --extra-index-url https://download.pytorch.org/whl/cu121

# ========== PYTORCH (CUDA 12.1) ==========
torch>=2.4.0
torchvision>=0.19.0
torchaudio>=2.4.0

# ========== BASE DEPENDENCIES ==========
-r requirements_base.txt
