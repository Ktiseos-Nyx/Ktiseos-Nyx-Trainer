{
  "is_builtin": true,
  "name": "malcolmrey (AdamW8bit)",
  "description": "malcolmrey's character recipe. AdamW8bit, dim/alpha 32/13 (low alpha), LR 1e-4 across the board, constant scheduler, 16 epochs, batch 8, scale_weight_norms 2.5, SNR 5.",
  "model_type": "SDXL",
  "resolution": 1024,
  "network_dim": 32,
  "network_alpha": 13,
  "unet_lr": 0.0001,
  "text_encoder_lr": 0.0001,
  "lr_scheduler": "constant",
  "optimizer_type": "AdamW8bit",
  "max_train_epochs": 16,
  "save_every_n_epochs": 2,
  "train_batch_size": 8,
  "gradient_checkpointing": true,
  "mixed_precision": "bf16",
  "min_snr_gamma": 5,
  "min_snr_gamma_enabled": true,
  "scale_weight_norms": 2.5,
  "clip_skip": 1,
  "max_token_length": 75,
  "keep_tokens": 0,
  "attribution": { "author": "malcolmrey", "type": "character", "base": "SDXL", "optimizer": "AdamW8bit" }
}
